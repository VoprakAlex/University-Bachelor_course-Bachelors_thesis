using TMPro;
using UnityEngine;
using UnityEngine.UI;
/*
public class BattleHud : MonoBehaviour
{
    public TMP_Text Name;

    public Slider HPSlider;
    public Slider SPSlider;

    public TMP_Text HPText;
    public TMP_Text SPText;

    public TMP_Text Pierce;
    public TMP_Text Slash;
    public TMP_Text Blunt;

    public void SetHud(Character unit)
    {
        Name.text = unit.Name;

        HPSlider.maxValue = unit.MaxHP;
        HPSlider.value = unit.CurrentHP;

        SPSlider.maxValue = unit.MaxSP;
        SPSlider.value = unit.CurrentSP;

        HPText.text = unit.CurrentHP + " / " + unit.MaxHP;
        SPText.text = unit.CurrentSP + " / " + unit.MaxSP;

        Pierce.text = unit.CurrentPierceDamageMultiplier.ToString();
        Slash.text = unit.CurrentSlachDamageMultiplier.ToString();
        Blunt.text = unit.CurrentBluntDamageMultiplier.ToString();
    }

}*/
