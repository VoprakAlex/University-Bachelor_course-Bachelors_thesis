using System;
using System.Collections.Generic;
using NUnit.Framework;
using UnityEngine;
/*
public class HandManager : MonoBehaviour
{
    public DeckManager DeckManager;

    public GameObject DicePrefab;

    public Transform HandTransform;

    public List<GameObject> DicesInHand = new List<GameObject>();

    public bool BattleReady = false;

    public GameObject BattleDiceObject = null;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

    }

    public void AddCardToHand(Dice DiceData)
    {
        GameObject NewDice = Instantiate(DicePrefab, HandTransform.position, Quaternion.identity, HandTransform);

        NewDice.GetComponent<DiceDisplay>().DiceData = DiceData;

        DicesInHand.Add(NewDice);

        
    }
}
*/